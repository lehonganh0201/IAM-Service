spring:
  config:
    import: optional:file:.env[.properties]

  application:
    name: ${APP_NAME}

  servlet:
    multipart:
      max-file-size: 5MB
      max-request-size: 10MB

  profiles:
    active: ${APP_ENV}